package StreamConcept.ShortCircuit;

import java.util.stream.Stream;

public class Limit {
    public static void main(String[] args) {
        /*
        *  Stream<T> limit(long n)
        * n is the no the stream should be limited
        * method retrive only first n objects and returns a new stream
        * */
    }
}
